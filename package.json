{
  "name": "site",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --no-fork",
    "dev:clean": "nuxt cleanup && nuxt dev --no-fork",
    "generate": "nuxt generate",
    "normalize:data": "node scripts/normalize-data.mjs",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "4.1.2",
    "vue": "^3.5.33",
    "vue-router": "^5.0.6"
  }
}
